# LaTeX 双向定位功能使用说明

## ✅ 功能已启用

您的 LaTeX 项目现在已经支持 PDF 和源代码之间的双向定位功能！

**✅ 参考文献问题已修复**：
- 修复了字符编码问题（`Đ` 字符）
- 添加了缺失的参考文献条目
- 修复了参考文献文件格式问题
- 参考文献现在正常显示

## 📁 生成的文件

- `visapp.pdf` - 生成的 PDF 文件
- `visapp.synctex.gz` - 同步文件（用于双向定位）

## 🎯 双向定位功能

### 从 PDF 到源代码
- 在 PDF 中点击任意位置
- 编辑器会自动跳转到对应的源代码位置

### 从源代码到 PDF
- 在源代码中点击任意位置
- PDF 会自动跳转到对应的输出位置

## 🛠️ 支持的编辑器

### 1. VS Code
- 安装扩展：LaTeX Workshop
- 配置文件已创建：`.vscode/settings.json`
- 使用方法：
  - 打开 `.tex` 文件
  - 按 `Ctrl+Alt+V` 打开 PDF 预览
  - 在 PDF 中双击任意位置跳转到源代码
  - 在源代码中按 `Ctrl+Alt+J` 跳转到 PDF

### 2. TeXstudio
- 内置支持双向定位
- 编译时自动生成同步文件
- 使用方法：
  - 打开 `.tex` 文件
  - 编译后按 `F7` 打开 PDF
  - 在 PDF 中点击跳转到源代码
  - 在源代码中按 `Ctrl+Click` 跳转到 PDF

### 3. TeXmaker
- 内置支持双向定位
- 使用方法：
  - 打开 `.tex` 文件
  - 编译后按 `F7` 打开 PDF
  - 在 PDF 中点击跳转到源代码
  - 在源代码中按 `Ctrl+Click` 跳转到 PDF

### 4. Vim/Neovim
- 需要安装 vimtex 插件
- 使用方法：
  - 打开 `.tex` 文件
  - 编译后使用 `:VimtexView` 打开 PDF
  - 在 PDF 中点击跳转到源代码
  - 在源代码中使用 `\ls` 跳转到 PDF

### 5. Emacs
- 需要安装 AUCTeX
- 使用方法：
  - 打开 `.tex` 文件
  - 编译后使用 `C-c C-v` 打开 PDF
  - 在 PDF 中点击跳转到源代码
  - 在源代码中使用 `C-c C-v` 跳转到 PDF

## 🔧 编译脚本

### 快速编译（带同步）
```bash
./quick_compile.sh
```

### 完整编译（带同步）
```bash
./compile.sh
```

### 专用同步编译
```bash
./compile_sync.sh
```

## ⚠️ 注意事项

1. **同步文件必须存在**：确保 `visapp.synctex.gz` 文件存在
2. **编辑器支持**：不是所有编辑器都支持双向定位
3. **编译选项**：必须使用 `-synctex=1` 选项编译
4. **文件路径**：确保 PDF 和源代码文件在同一目录

## 🎉 使用效果

现在您可以：
- 在 PDF 中点击某个段落，编辑器会自动跳转到对应的源代码
- 在源代码中点击某个位置，PDF 会自动跳转到对应的输出位置
- 大大提高编辑和调试效率

## 📝 故障排除

如果双向定位不工作：
1. 检查 `visapp.synctex.gz` 文件是否存在
2. 确保使用支持双向定位的编辑器
3. 重新编译文档
4. 检查编辑器设置

享受高效的 LaTeX 编辑体验！
